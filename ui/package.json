{
  "name": "mcp-delightloop-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "build:all": "pnpm run build:campaign-details && pnpm run build:campaign-list && pnpm run build:contact-list && pnpm run build:contact-card && pnpm run build:recipient-list",
    "build:campaign-details": "vite build --mode campaign-details",
    "build:campaign-list": "vite build --mode campaign-list",
    "build:contact-list": "vite build --mode contact-list",
    "build:contact-card": "vite build --mode contact-card",
    "build:recipient-list": "vite build --mode recipient-list",
    "dev": "vite",
    "preview": "vite preview"
  },
  "dependencies": {
    "@modelcontextprotocol/ext-apps": "^1.7.2",
    "@untitledui/icons": "^0.0.22",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "tailwind-merge": "^3.3.1"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "@types/node": "^20.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.0",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vite-plugin-singlefile": "^2.1.0"
  }
}
